# 📖 네이버 키워드 기회 분석기 - 사용법

## 🔑 1. API 설정 (필수!)

### 1-1. 네이버 API 키 발급

1. **네이버 개발자센터** 접속: https://developers.naver.com/
2. **로그인** → 우측 상단 **Application** 클릭
3. **애플리케이션 등록** 버튼 클릭
4. 정보 입력:
   - **애플리케이션 이름**: 키워드분석기 (아무거나)
   - **사용 API 선택**:
     - ✅ **검색** (필수)
     - ✅ **데이터랩 (검색어 트렌드)** (필수)
     - ✅ **데이터랩 (쇼핑인사이트)** (선택)
   - **환경 추가**: 웹 설정 → http://localhost
5. **등록 완료** 후 **Client ID**와 **Client Secret** 복사

### 1-2. .env 파일 만들기

프로젝트 폴더에 `.env` 파일 생성:

**Windows:**
```cmd
notepad .env
```

**Mac/Linux:**
```bash
nano .env
```

**내용 입력:**
```
NAVER_CLIENT_ID=발급받은_클라이언트ID
NAVER_CLIENT_SECRET=발급받은_시크릿
TZ=Asia/Seoul
```

저장하고 종료!

---

## 🚀 2. 기본 사용법

### 방법 1: GUI 메뉴로 카테고리 선택 (추천!)

```bash
python main.py --interactive
```

또는 짧게:
```bash
python main.py -i
```

**실행 화면:**
```
============================================================
📂 네이버 쇼핑 카테고리 선택
============================================================
  1. 패션의류 (50000000)
  2. 패션잡화 (50000001)
  3. 화장품/미용 (50000002)
  4. 디지털/가전 (50000003)
  5. 가구/인테리어 (50000004)
  6. 출산/육아 (50000005)
  7. 식품 (50000006)
  8. 스포츠/레저 (50000007)
  9. 생활/건강 (50000008)
  10. 여가/생활편의 (50000009)
  11. 면세점 (50000010)
  0. 카테고리 없이 진행
============================================================

선택 (번호 입력):
```

원하는 번호 입력하면 자동 실행!

---

### 방법 2: 명령어로 직접 지정

```bash
# 패션의류 카테고리 분석
python main.py --category 50000000

# 화장품/미용 카테고리, 30일 분석
python main.py --category 50000002 --period 30

# 디지털/가전, 브랜드만 100개
python main.py --category 50000003 --mode brand --top-n 100
```

---

## 📋 3. 고급 옵션

### 3-1. 분석 기간 설정

```bash
# 7일 분석 (기본)
python main.py -i --period 7

# 30일 분석
python main.py -i --period 30

# 90일 분석
python main.py -i --period 90
```

### 3-2. 출력 개수 조절

```bash
# 상위 50개 (기본)
python main.py -i --top-n 50

# 상위 100개
python main.py -i --top-n 100

# 상위 200개
python main.py -i --top-n 200
```

### 3-3. 브랜드/상품 선택

```bash
# 브랜드 + 상품 둘 다 (기본)
python main.py -i --mode both

# 브랜드만
python main.py -i --mode brand

# 상품만
python main.py -i --mode product
```

### 3-4. 블로그 지표 포함

```bash
python main.py -i --with-blog
```

### 3-5. 커스텀 키워드 파일 사용

**keywords.txt 파일 만들기:**
```
나이키
아디다스
뉴발란스
운동화
스니커즈
티셔츠
```

**실행:**
```bash
python main.py -i --seed-file keywords.txt
```

### 3-6. 출력 폴더 변경

```bash
python main.py -i --outdir ./결과물
```

---

## 🎯 4. 실전 예시

### 예시 1: 패션의류 트렌드 찾기

```bash
python main.py -i
# 메뉴에서 1번 선택 (패션의류)
```

### 예시 2: 화장품 브랜드 기회 키워드 상위 100개

```bash
python main.py --category 50000002 --mode brand --top-n 100
```

### 예시 3: 스포츠/레저 30일 분석 + 블로그 포함

```bash
python main.py --category 50000007 --period 30 --with-blog
```

### 예시 4: 내 키워드 리스트로 분석

```bash
# my_keywords.txt 파일 준비
python main.py -i --seed-file my_keywords.txt --period 14
```

---

## 📊 5. 결과 확인

실행 완료 후 `out/` 폴더에 CSV 파일 생성:

```
out/
├── brand_trends_50000000.csv      # 브랜드 키워드
└── product_trends_50000000.csv    # 상품 키워드
```

### CSV 파일 열기

- **Windows**: Excel로 열기
- **Mac**: Numbers 또는 Excel로 열기
- **Linux**: LibreOffice Calc로 열기

### CSV 컬럼 설명

| 컬럼 | 의미 |
|------|------|
| **keyword** | 키워드명 |
| **type** | brand(브랜드) / product(상품) |
| **search_recent_avg** | 최근 기간 검색량 평균 |
| **search_prev_avg** | 이전 기간 검색량 평균 |
| **search_wow** | 검색량 증감률 (높을수록 급상승) |
| **shopping_total** | 네이버 쇼핑 전체 상품 수 |
| **smartstore_est** | 스마트스토어 추정 개수 |
| **demand_z** | 수요 점수 (상대적 검색량) |
| **momentum** | 모멘텀 (트렌드 상승세) |
| **opportunity_score** | 🎯 **기회 점수** (높을수록 기회!) |

**opportunity_score가 높을수록** = 검색은 많은데 공급은 적음 = 💰 기회!

---

## ⚠️ 6. 문제 해결

### 문제 1: "NAVER_CLIENT_ID must be set"

→ `.env` 파일이 없거나 잘못됨. 위 **1. API 설정** 다시 확인

### 문제 2: "Authentication error: 401"

→ API 키가 잘못됨. 네이버 개발자센터에서 다시 확인

### 문제 3: "Rate limit hit"

→ API 호출 제한. 잠시 기다렸다가 재실행

### 문제 4: CSV 파일 한글 깨짐 (Excel)

→ Excel에서:
1. **데이터** 탭 → **텍스트/CSV에서** 선택
2. 인코딩을 **UTF-8** 선택
3. 가져오기

---

## 💡 7. 팁

### 팁 1: 적은 키워드로 먼저 테스트

```bash
# 기본 20개 키워드로 빠른 테스트
python main.py -i --top-n 20
```

### 팁 2: API 호출 최소화

- 키워드가 많으면 시간이 오래 걸림
- 처음엔 `--top-n 20~50` 으로 테스트
- 나중에 100~200으로 확대

### 팁 3: 결과 해석

**높은 opportunity_score = 기회!**

- **demand_z 높음** = 많이 찾는 키워드
- **smartstore_est 낮음** = 경쟁 적음
- **momentum 양수** = 상승 중

**예시:**
```
keyword: "골프웨어"
demand_z: 2.1 (검색 많음)
smartstore_est: 1200 (공급 적음)
momentum: 0.45 (급상승 중)
opportunity_score: 0.89 → 💰 기회!
```

---

## 📞 8. 도움말

### 전체 옵션 보기

```bash
python main.py --help
```

### 카테고리 코드

| 코드 | 카테고리 |
|------|----------|
| 50000000 | 패션의류 |
| 50000001 | 패션잡화 |
| 50000002 | 화장품/미용 |
| 50000003 | 디지털/가전 |
| 50000004 | 가구/인테리어 |
| 50000005 | 출산/육아 |
| 50000006 | 식품 |
| 50000007 | 스포츠/레저 |
| 50000008 | 생활/건강 |
| 50000009 | 여가/생활편의 |
| 50000010 | 면세점 |

---

## 🎉 빠른 시작 (요약)

```bash
# 1. API 키 발급 후 .env 파일 만들기
notepad .env

# 2. 패키지 설치
pip install -r requirements.txt

# 3. 실행!
python main.py -i

# 4. 원하는 카테고리 번호 입력

# 5. out/ 폴더에서 CSV 확인
```

끝! 🚀
